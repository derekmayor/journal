export interface Props {
  noPadding?: boolean;
  "aria-label"?: string;
}

const { noPadding = false, "aria-label": ariaLabel } = Astro.props;
---

<hr class="border-skin-line" aria-hidden={true} />

<style>
  hr {
    @apply border-0 border-t border-skin-line;
  }
</style>
