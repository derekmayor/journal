---
export interface Props {
  noPadding?: boolean;
  ariaLabel?: string;
}

const { noPadding = false, ariaLabel } = Astro.props;
---

<hr class="border-border" aria-label={ariaLabel} />

<style>
  @reference "../styles/global.css";

  hr {
    @apply border-0 border-t border-border;
  }
</style>
