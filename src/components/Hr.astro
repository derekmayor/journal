---
export interface Props {
  noPadding?: boolean;
  ariaLabel?: string;
}

const { noPadding = false, ariaLabel } = Astro.props;
---

<hr class="border-skin-line" aria-label={ariaLabel} />

<style>
  hr {
    @apply border-0 border-t border-skin-line;
  }
</style>
